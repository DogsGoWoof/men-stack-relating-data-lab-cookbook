<%- include('../partials/_head-style.ejs') %>
        <title>New Food</title>
    </head>

    <%- include('../partials/_cupboard-open-close.ejs') %>

        <%- include('../partials/_navbar.ejs') %>
        <h1><%= food.name %></h1>
            <div class="content-container">
                <a href="/users/<%= user._id %>/foods/<%= food._id %>/edit"><div class="link-div yellow">Edit Food</div></a>
                <form action="/users/<%= user._id %>/foods/<%= food._id %>?_method=DELETE" method="POST">
                    <button type="submit"><div class="link-div red">Delete Food</div></button>
                </form>
            </div>
    </body>
    
</html>